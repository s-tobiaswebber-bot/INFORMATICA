#include<stdio.h>
#include<stdlib.h>

int funzioneStringa(int var){
    char *ptr=(char*)malloc(sizeof(char)*x+1);
    return ptr;
}


int main(){
    int var=5;
    char *ptr;
    ptr = funzioneStringa(var);
    return 0;
}